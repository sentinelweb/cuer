PRAGMA foreign_keys = 'ON';

CREATE TABLE image(
  id INTEGER NOT NULL PRIMARY KEY ,
  url TEXT NOT NULL,
  width INTEGER,
  height INTEGER
);

insertOrReplace:
INSERT OR REPLACE INTO image(
  id,
  url,
  width,
  height
)
VALUES (?, ?, ?, ?);