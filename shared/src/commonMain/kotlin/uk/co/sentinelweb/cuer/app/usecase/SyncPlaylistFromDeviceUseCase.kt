package uk.co.sentinelweb.cuer.app.usecase

import uk.co.sentinelweb.cuer.app.orchestrator.OrchestratorContract

class SyncPlaylistFromDeviceUseCase() {

    fun syncPlaylist(remoteId: OrchestratorContract.Identifier<String>) {
        // val remoteIdWithLocator = OrchestratorContract.Source.LOCAL_NETWORK(loc)
    }
}